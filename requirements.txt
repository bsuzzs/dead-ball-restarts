# Versions used in the validated full run (Python 3.14.7).
numpy==2.5.2
pandas==3.0.5
scipy==1.18.1
statsmodels==0.15.0
scikit-learn==1.9.0
matplotlib==3.11.1
shap==0.52.0
torch==2.14.0
# pyarrow is optional; without it the pipeline uses its CSV cache fallback.
